#!/bin/sh
# Pre-commit hook para verificar build antes de commit

echo "🔍 Ejecutando verificación pre-commit..."

# Ejecutar build check
npm run build:check

if [ $? -ne 0 ]; then
    echo "❌ Build falló, commit cancelado"
    exit 1
fi

echo "✅ Build exitoso, procediendo con commit"